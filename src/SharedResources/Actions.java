package SharedResources;

/**
 * This enum represents the possible actions that a client can take using requests to the server
 */
public enum Actions {
	GET_ALL_EVENTS,
	ADD_EVENT,
	DELETE_EVENT,
	GET_CURRENT_EVENTS,
	GET_OLD_EVENTS,
	MARK_COMPLETED,
	DONATE;
}
